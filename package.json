{
  "name": "ai-copilot-docs",
  "private": true,
  "description": "GitBook-style Markdown; preview locally with HonKit",
  "scripts": {
    "start": "honkit serve",
    "serve": "honkit serve",
    "build": "honkit build",
    "build:demos": "node scripts/generate-demo-gifs.mjs"
  },
  "devDependencies": {
    "gifenc": "^1.0.3",
    "honkit": "^5.1.5",
    "honkit-plugin-mermaid": "^0.1.4",
    "honkit-plugin-search-plus": "^1.0.0",
    "pngjs": "^7.0.0"
  }
}
